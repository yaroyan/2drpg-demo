using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum StatusParameters
{
    HP, SP, MP,
    STR, DEF, VIT,
    INT, RES,
    DEX,
    AGI,
    LUK,
    NONE,
}
